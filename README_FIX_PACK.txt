MARKET RADAR FIX PACK

Overwrite these files into your live project folder, then push to GitHub.

Included fixes:
- account page context repaired
- login/signup flow cleaned up with readable errors
- login no longer silently creates missing accounts
- local_factor field restored on dashboard form
- barcode scanner improved and given status feedback
- generated listings preserved
- best platform box added back to analyzer flow
- mobile/auth styling tightened

Deploy steps:
1. unzip into project root
2. overwrite existing files
3. git add .
4. git commit -m "Launch-day account and dashboard fixes"
5. git push
